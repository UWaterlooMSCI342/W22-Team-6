<h3>Filter</h3>

<%= form_with(url: path, method: :get, local: true) do |form| %>
  <div class="field">
    <%= form.label :first_name, "First name" %>
    <%= form.text_field :first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name, "Last name" %>
    <%= form.text_field :last_name %>
  </div>

  <div class="field">
    <%= form.label :team_name, "Team" %>
    <%= form.select :team_name, Team.team_names, :include_blank => true %>
  </div>
  
  <div class="field">
    <%= form.label :participation_rating %>
    <!-- <%= form.select :participation_rating, Feedback::CHOICES, :include_blank => true %> -->

    <!-- <%= form.collection_check_boxes :participation_rating, Feedback.choices, :first, :last, :include_hidden => false do |pr| %> -->
      <!-- <div id="checkbox"><%= pr.check_box %><%= pr.label {pr.text} %></div> -->
    <!-- <% end %> -->

    <!-- dropdown that allows multiple selections, but requires CTRL-click or SHIFT-click -->
    <!-- <%= form.collection_select :participation_rating, Feedback.choices, :first, :last, {}, {:multiple => true, :size => Feedback::CHOICES.length} %> -->

    <!-- <%= field_set_tag  do %> -->
      <%= form.select :participation_rating_start, Feedback::CHOICES, :include_blank => true %>
      <%= form.select :participation_rating_end, Feedback::CHOICES, :include_blank => true %>
    <!-- <% end %> -->
  </div>
  

  <div class="field">
    <%= form.label :effort_rating %>
    <!-- <%= form.select :effort_rating, Feedback::CHOICES, :include_blank => true %> -->

    <!-- <%= form.collection_check_boxes :effort_rating, Feedback.choices, :first, :last, :include_hidden => false do |pr| %> -->
      <!-- <div id="checkbox"><%= pr.check_box %><%= pr.label {pr.text} %></div> -->
    <!-- <% end %> -->

    <%= form.select :effort_rating_start, Feedback::CHOICES, :include_blank => true %>
    <%= form.select :effort_rating_end, Feedback::CHOICES, :include_blank => true %>
  </div>

  <div class="field">
    <%= form.label :punctuality_rating %>
    <!-- <%= form.select :punctuality_rating, Feedback::CHOICES, :include_blank => true %> -->

    <!-- <%= form.collection_check_boxes :punctuality_rating, Feedback.choices, :first, :last, :include_hidden => false do |pr| %> -->
      <!-- <div id="checkbox"><%= pr.check_box %><%= pr.label {pr.text} %></div> -->
    <!-- <% end %> -->

    <%= form.select :punctuality_rating_start, Feedback::CHOICES, :include_blank => true %>
    <%= form.select :punctuality_rating_end, Feedback::CHOICES, :include_blank => true %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.select :priority, Feedback::PRIORITY_LEVEL.invert, :include_blank => true %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_field :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_field :end_date, value: Date.today %>
  </div>

  <div class="actions">
    <%= form.submit "Filter" %>
  </div>
<% end %>
