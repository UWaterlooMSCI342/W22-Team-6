<div style="text-align: center;">
  <%= will_paginate model %>
</div>

<%= form_with(method: :get, local: true) do |form| %>
  <%= form.select :per_page, options_for_select([1, 5, 10, 20, 100], model.per_page), {}, { onchange: 'this.form.submit();' } %>
<% end %>